import { NextResponse } from 'next/server'

// POST /api/csv
// Recebe o CSV com colaboradores e importa para o banco
export async function POST() {
  // Implementação na próxima etapa
  return NextResponse.json({ message: 'em construção' }, { status: 501 })
}
